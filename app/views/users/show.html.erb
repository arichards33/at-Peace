<h1>User Profile</h1>
<hr>

<% user = User.find_by(id: params["id"]) %>

<%if @current_user.id == session[:user_id] %>

<p>Name: <%= user.name %></p>
<p>Email: <%= user.email %></p>
<p><%= link_to "Edit Account Info", "/users/#{user.id}/edit" %></p>
<% end %>
